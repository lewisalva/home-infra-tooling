apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infra-server-pg.fullname" . }}-test-connection"
  labels:
    {{- include "infra-server-pg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infra-server-pg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
